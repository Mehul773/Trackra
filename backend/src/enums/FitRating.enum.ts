/**
 * How well the job matches the user's skill profile.
 * Gemini AI returns one of these values after analyzing the JD.
 */
export enum FitRating {
  STRONG = 'STRONG',
  STRETCH = 'STRETCH',
  WEAK = 'WEAK',
}
